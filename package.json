{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "php artisan serve",
        "build": "vite build",
        "migrate:apply": "php artisan migrate",
        "migrate:rollback": "php artisan migrate:rollback",
        "migrate:create": "php artisan migrate:create"
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
